{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \\\\\\\"Error: no test specified\\\\\\ && exit 1",
    "buidl:ui": "rm -rf dist && cd ../phonebook && run run build && cp -r dist ../phonebook_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuid && git push"
  },
  "author": "Mike Ly",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
